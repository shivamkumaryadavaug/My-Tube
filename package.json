{
  "name": "mytube",
  "version": "1.0.0",
  "private": true,
  "description": "MYTUBE — Your YouTube. Your Playlist. Your Focus.",
  "scripts": {
    "cap:prepare": "node scripts/prepare-capacitor.mjs",
    "cap:sync": "npm run cap:prepare && npx cap sync android",
    "android:add": "npx cap add android",
    "android:build": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0"
  }
}
